---
name: memory-fork-routing
description: 本地 memory 控制方法论脚手架。关掉不可控的云端记忆后，用「六分叉路由 + 路由指针」把主 MEMORY.md 瘦身为纯路由层，再用「memory 台账 + 定期人工审核」保证有效性。本 skill 触发后通过**会话式半自动引导**：开头先让你选起点（迁移旧记忆 / 新用户从零填），逐项给模板+脱敏示例，由你填写/确认后再落盘，不静默代写，填写总量封顶以防冗长。适用于想自己掌控 Agent 记忆、不依赖评分/权重机制的 WorkBuddy 类本地 memory 体系。触发词：整理记忆 / 记忆重构 / 六分叉 / memory 路由 / 关云端记忆 / 建 memory 台账 / 记忆瘦身。
---

# Memory Fork Routing —— 本地记忆控制脚手架（半自动引导版）

一个冷知识：**memory 有效性不能用有效性评分来管理。** 本 skill 提供一套「关云端 → 六分叉路由 → 台账人工审核」的方法论，并把落地过程变成**一次引导式会话**：先铺空骨架，再逐项给你模板和脱敏示例，你确认后我才写进文件。它**不替你臆造内容**，只把抽屉建好、把填写过程陪你走完。

## 核心思想（三件事）

1. **关掉不可控的云端记忆。** 云端记忆无法精确控制、每晚自动从上下文挑选内容生成「用户画像」且越攒越长；而 transformer 至今难以处理好「用户禁止某事时的注意力」——你越强调别提，它越记得清。所以第一原则：核心记忆只留在本地，别让 AI 碰云端那部分。
2. **六分叉路由（分叉树 + 路由指针）。** 按「触发后我要做的那个动作」把 memory 触发分成若干分叉；上级节点必须完全包含下级节点；主 `MEMORY.md` 只放**路由指针**，规则与行为全部下沉到 `knowledge/01~06` 二级文件。基础红线必须每次会话都加载——靠 `SessionStart` hook 强注入，不依赖模型自觉。
3. **memory 台账 + 人工审核。** 记忆的有效运行依赖「触发条件 + 操作规则」两部分，而这两部分是自然语言，必有漏洞。必须有一套自检机制判断「规则注入了但这次仍犯错」为何失效。**memory 修复必须人工审核**，否则就和关掉的云端那套没区别。有效性不能靠评分/权重管理——最有效的那条 memory 往往是永远静默生效的那条。

## 适用对象与前置

- 适用于 **WorkBuddy 类「本地文件 +（可选）云端」混合 memory** 的客户端；跨客户端不保证通用。
- 本 skill **不自动关闭你的云端记忆**（那是改你设置、且不同客户端开关位置不同）。仅在阶段 C 给手动关闭指引。
- 需要 WorkBuddy 支持 `SessionStart` hook（已验证 schema，见阶段 C）。

---

## 执行流程（总览）

触发本 skill 后，按以下顺序推进。**不要一次性把所有内容倒给用户**——铺完骨架后，先让用户选起点分支，再每次只推进一个区块，确认后再落盘。

- **阶段 0｜环境探测（必走第一步）**：实际执行命令确认当前 OS（macOS / Windows / Linux），**禁止凭会话默认假设**。后续所有 shell 命令一律按探测到的 OS 选方言（见下方「命令方言对照表」）。
- **阶段 0.5｜冲突自检（若已有 MEMORY.md）**：读你现有 MEMORY.md/红线，预判本 skill 要做的操作（写文件/改 settings/执行 shell）会不会被你拦；会则提前协商降级，避免跑到一半卡住。
- **阶段 A｜铺骨架（自动）**：把 `assets/` 下的空模板复制到 `~/.workbuddy/`（复制按 OS 选 `cp -r` / `xcopy` / 文件工具），已存在则先备份，不静默覆盖。
- **阶段 B0｜选择起点（分支）**：用 `AskUserQuestion` 让用户二选一——已有大量旧记忆（A 路径：贴给我按新抽屉重新归类）还是新用户/记忆很少（B 路径：从零引导填）。
- **阶段 B｜会话引导填写（半自动，封顶）**：
  - **A 路径（迁移旧记忆）**：用户把现有个人记忆贴给我，我按六分叉抽屉重新归类存放；归好后再引导关云端。
  - **B 路径（新用户）**：我引导从零填，**总量封顶**——硬红线 3–5 条 + 分叉示例规则最多 6 条（可跨任意分叉，不必每个分叉穷尽），凑齐即结束，避免冗长。
- **阶段 C｜关云端 + hook（指引）**：给手动关云端的步骤；hook 配置块默认只提示，若你明确授权则带备份合并进 `settings.json`。

---

## 阶段 0：环境探测（必走第一步，禁止假设）

**这是整个流程的第一条动作，先于阶段 A。** 模型常会默认自己跑在 Linux 上、吐出 `ls` / `cp` 等 Windows 不认的指令——必须先**实际执行**命令确认 OS，再决定后续所有命令的方言，绝不凭会话默认值假设。

**探测（实际执行，不是猜）**：按以下顺序跑，以第一个返回真实值（非报错）的为准，并把结果记为 `OS`：
1. `python3 -c "import platform,sys; print(platform.system())"` → 返回 `Darwin`(macOS) / `Windows` / `Linux`
2. 失败则 `python -c "import platform; print(platform.system())"`
3. 再失败（无 Python）则 shell：`uname -s`（macOS=`Darwin`、Linux=`Linux`）；Windows 跑 `cmd /c ver` 或 PowerShell `Get-CimInstance Win32_OperatingSystem | Select-Object Caption`

**得到 `OS` 后，全程遵守下方命令方言表。任何涉及 shell 的操作都按表的对应列来，绝不混用、绝不默认 Linux。**

### 命令方言对照表（按 OS 取用）

| 操作 | macOS | Windows | Linux |
|---|---|---|---|
| 复制 assets → `~/.workbuddy` | `cp -r "<src>" "<dst>"` | `xcopy "<src>" "<dst>" /E /I` 或 直接用文件工具按绝对路径 Write | `cp -r "<src>" "<dst>"` |
| 备份现有文件 | `cp "<f>" "<backup>/"` | `copy "<f>" "<backup>\"` 或 文件工具 | `cp "<f>" "<backup>/"` |
| 弹开文件位置（B0-A） | `open -R "<文件>"` | `explorer.exe /select,"<文件>"` | `xdg-open "<目录>"` |
| hook `command` 字段 | `"/Users/<u>/.workbuddy/hooks/session-start-inject.py"` | `"python" "C:\\Users\\<u>\\.workbuddy\\hooks\\session-start-inject.py"` | `"/home/<u>/.workbuddy/hooks/session-start-inject.py"` |
| 列出/检查目录（如确需） | `find <dir> -maxdepth 1` | `Get-ChildItem <dir>` 或 用模型文件工具 | `find <dir> -maxdepth 1` |

> **强烈建议**：阶段 A 的「复制文件」优先用**模型的文件工具（Read/Write/Edit 直接落到绝对路径）**，不依赖 shell；若必须用 shell 复制，严格按上表 OS 列选命令。**禁止用 `ls` 列目录**（Windows 不认）——改用模型文件工具或上表对应命令。
>
> **路径 `~` 的展开**：macOS/Linux = `$HOME`；Windows = `C:\Users\<用户名>`，且所有反斜杠在 JSON / `command` 字符串里需转义为 `\\`。

---

## 阶段 0.5：冲突自检（若本地已有 MEMORY.md，必走；新用户跳过）

**目的**：本 skill 要写你的 `MEMORY.md`、`knowledge/*`、台账，还可能改 `settings.json`、执行 shell——这些正好都在「用户红线」的管辖区。若你的红线拦了其中某项，硬跑会卡在半路。动文件前先预判、先协商。

**触发**：阶段 0 拿到 `OS` 后，若 `~/.workbuddy/MEMORY.md` **已存在**则走本步；若不存在（新用户），直接跳到阶段 A。

**动作**：
1. `Read` 现有 `MEMORY.md`（及若存在的红线/规则文件），识别其中的「操作约束类红线」——即约束 AI *动作*的条款，典型关键词：禁止静默写盘 / 不擅自改文件 / 不改配置 / 不执行 shell / 不读记忆 / 外发须授权。
2. 把本 skill 将执行的操作与上述红线逐条比对：
   - 写 `MEMORY.md` 路由层（阶段 A + B1）→ 是否被「禁止写记忆文件」拦
   - 写 `knowledge/01~06`（B2）→ 同上
   - 写台账（B4）→ 同上
   - 复制文件（阶段 A，shell `cp`/`xcopy` 或文件工具）→ 是否被「禁止执行 shell / 禁止写盘」拦
   - 弹开文件夹（B0-A，shell `open`/`explorer`/`xdg-open`）→ 是否被「禁止执行 shell」拦
   - 改 `settings.json` 合并 hook（阶段 C，需授权）→ 是否被「禁止改配置」拦
   - 读现有 `MEMORY.md`（本步）→ 是否被「禁止读记忆」拦
3. **若有潜在冲突，先亮给用户并给降级方案，确认后再进阶段 A**（用自然语言或 `AskUserQuestion`）。降级映射：

   | 你的红线拦了 | 降级处理 |
   |---|---|
   | 静默写 / 改我的记忆文件 | B 填写阶段我只给【模板+示例】，由你亲手复制进文件，我不代 `Write/Edit` |
   | 禁止执行 shell 命令 | 阶段 A 复制改用模型文件工具直接 `Write` 到绝对路径；B0 弹开改为「给你路径，你自己开文件管理器」 |
   | 禁止改配置文件 / settings.json | 阶段 C 仅打印配置块提示，不合并（默认已如此） |
   | 禁止读我的记忆文件 | 跳过 A 路径自动归类；改为「你主动贴内容给我，我才处理」 |

4. **若无冲突**：简短汇报「未发现会与你的红线冲突的操作」，直接进阶段 A。

> 这一步本身就是「尊重你既有规则」的体现——被拦不是缺陷，提前协和才是正确体验。A 路径（迁移旧记忆）天然更易撞线（要重写 MEMORY.md），B 路径（新用户）多无冲突源。

---

## 阶段 A：铺骨架（自动）

调用时，**先确认阶段 0 已得到 `OS`**。按以下顺序把 `assets/` 复制到 `~/.workbuddy/`（**复制命令按 OS 从「命令方言对照表」取：macOS/Linux 用 `cp -r`、Windows 用 `xcopy /E /I`；或优先用模型文件工具直接 Write 到绝对路径**），已存在则先备份，绝不静默覆盖现有文件：

1. 若 `~/.workbuddy/MEMORY.md` 已存在，先备份到 `~/.workbuddy/_backup-<日期>/`，再写入路由型 `MEMORY.md` 骨架。
2. `assets/knowledge/01-dialogue.md … 06-system.md` → `~/.workbuddy/knowledge/`（已存在则备份）。
3. `assets/memory-usage-ledger.md` → `~/.workbuddy/`（已存在则备份）。
4. `assets/hooks/session-start-inject.py` → `~/.workbuddy/hooks/`（已存在则备份）。

铺完后，向用户简短汇报「已建好哪些空抽屉」，然后从 **B0** 开始引导（先让用户选起点分支，再进具体填写）。

---

## 阶段 B：会话引导填写（半自动，封顶）

**行为纪律（必须遵守）**：

- **先走 B0 选分支**：阶段 A 铺完骨架后，必须先让用户二选一（已有旧记忆 / 新用户），再进入具体填写。
- 对每个区块：先输出该区块的【用途说明 + 填写模板 + 脱敏示例】，再提问让你填写/确认。
- **不臆造内容**：你的红线、规则必须来自你的回答（A 路径下来自你贴的旧记忆）；你答得太笼统（如「别乱来」）时，要追问到能写出具体场景+后果为止。
- **确认后再落盘**：你给出内容后，先复述一遍要点，待你说「确认 / 就这样 / 写吧」再 `Write/Edit`。
- **总量封顶（关键，防冗长）**：最终落到文件里的「规则」**最多 6 条示例**（可跨任意分叉，不必每个分叉都穷尽）；硬红线单独建议 3–5 条。凑齐即结束，不要逼用户把六个分叉都填满。
- 结构化多选题（如「选起点分支」「强制注入哪些分叉」）用 `AskUserQuestion`；开放式内容（红线、规则）用自然语言提问。

### B0 选择起点分支（必走，二选一）

阶段 A 铺完骨架后，用 `AskUserQuestion` 问用户属于哪种情况：

- **A. 我已有大量个人记忆（想迁移）**：个人记忆文件在 `~/.workbuddy/MEMORY.md`（以及当前项目的 `<项目路径>/.workbuddy/memory/`）。**选了 A，先在你的电脑上把这两个位置用文件管理器弹开**（隐藏文件夹一般人找不到），你直接复制内容贴给我即可——模型要**实际执行**下面的命令，不是只把命令告诉你：
  - 以下弹开命令**按阶段 0 探测到的 `OS` 选对应行，不要凭默认假设**：
  - **macOS**：`open -R ~/.workbuddy/MEMORY.md`（在 Finder 中定位并高亮该文件）；当前项目记忆目录用 `open "<项目路径>/.workbuddy/memory/"`。
  - **Windows**：先把 `~` 展开为真实 home（如 `C:\Users\<用户名>`），再 `explorer.exe /select,"C:\Users\<用户名>\.workbuddy\MEMORY.md"`（在资源管理器中定位并选中该文件）；项目目录同理。
  - **Linux**：`xdg-open ~/.workbuddy`。
  - 我会按新的六分叉「抽屉」把你贴来的内容逐条重新归类存放；**归好之后你再关闭云端记忆**。
  - *（若客户端自带「记忆」查看面板，也可从那里复制；没有就直接用我弹开的文件路径最可靠。）*
- **B. 我是新用户 / 记忆很少 / 不想保留旧记忆**：我提供引导，你来填写 memory 的主要功能（硬红线 + 最多 6 条分叉示例规则），快速完成安装。

**A 路径后续**：用户把现有内容贴来后，我先通读、逐条给出「这条归到哪个抽屉（硬红线 / 分叉①~⑥）」的归类方案并复述，用户确认后写入对应文件；归好后再走 B3/B4 与阶段 C。
**B 路径后续**：从 B1 起引导从零填写，遵守总量封顶。

### B1 每会话硬红线 → `~/.workbuddy/MEMORY.md`

**用途**：这些是不靠条件触发、每次会话都必须加载的底线（如「不主动发邮件」「写盘须明确动词+点名文件」）。

**模板**：
```
# 每会话硬红线（须每次加载，不靠条件触发）
1. <一句话规则> —— <具体场景/后果>
2. ...
```

**脱敏示例**（仅示意格式，非你的真实内容）：
```
1. 不主动发送邮件——除非当条指令明确说"发/发出去"。
2. 写盘须同时具备「明确动词 + 用户点名的文件」两个锚，缺一即越界。
```

**引导动作**：问用户「你平时最不希望我做的几件事是什么？每条最好带上具体场景和后果。」收集后复述、确认、写入 `MEMORY.md` 红线段。（此步主要针对 **B 路径**从零填；**A 路径**的红线在 B0 重新归类时一并处理，不必重复走这步。）

### B2 六分叉规则 → `~/.workbuddy/knowledge/01~06`

**用途**：按「触发后我要做的那个动作」分叉，细则全在这里，主 MEMORY 只放指针。上级节点须完全包含下级。

**每分叉模板**：
```
# 分叉①：对话与判断
触发条件：我要"说"一句话（回答/提问/反驳/要你拍板/下结论）
规则：
- <规则，带场景+后果>
- <规则>
红线落点：<若此类有红线，注明编号与文件>
```

**分叉清单（逐项移到）**：

| 文件 | 分叉 | 触发动作 |
|---|---|---|
| `01-dialogue.md` | ① 对话与判断 | 我要「说」一句话（回答/提问/反驳/要你拍板/下结论前先核实） |
| `02-output.md` | ② 产出与交付 | 我要「造」一个东西（文稿/文件/PPT/对照表/交付物） |
| `03-calc.md` | ③ 计算与研判 | 我要「算」出一个数、给量化结论 |
| `04-local.md` | ④ 本机与外部动作 | 我要「动」现实（命令/操作 App/外设/发信/上传发布外发/部署） |
| `05-lookup.md` | ⑤ 事实检索 | 我要「查」事实 / 引用脚本报出的数字前先回源核真值 |
| `06-system.md` | ⑥ 系统自维护 | 我要「改我自己」（记忆/skill/知识库结构） |

**脱敏示例**（仅 `01` 示意）：
```
# 分叉①：对话与判断
触发条件：我要"说"一句话
规则：
- 下"有/没有""对/错"这类结论前先核实，不凭印象开口。
- 要用户拍板时必须交代来龙去脉、待决原文、可选项与后果、我的建议。
红线落点：#11（拍板六要素）→ MEMORY.md
```

**引导动作**：向用户说明六个分叉的用途（见上表），然后请他给出**最多 6 条**想立的规则/红线，**可跨任意分叉、不必每个分叉都填**。每条我判断归属分叉→复述→确认→写入对应 `knowledge/0X` 文件。凑齐 6 条或用户说「够了」即停止，进入 B3。

### B3 强制注入分叉 → 改 `session-start-inject.py` 的 `FILES`

**用途**：默认只强制注入 `MEMORY.md` 路由层。你希望哪些分叉**不靠模型自觉、每次必加载**，就加进脚本的 `FILES` 列表。

**引导动作**：用 `AskUserQuestion`（multiSelect）问「哪些分叉要强制每次注入？」选项为 ①~⑥ + 仅路由层(MEMORY)。用户选定后，编辑 `FILES` 列表并复述改动，待确认。

> 注意：强制注入越多，每次会话上下文越长。建议只把「基础红线 + 最高频分叉」强制加载，其余按需读。

### B4 启用台账 → `~/.workbuddy/memory-usage-ledger.md`

**用途**：每条红线命中/拦截追加一行，供每周人工审核。

**格式**（模板里已有表头）：
```
YYYY-MM-DD | R<编号> | hit/block | <场景>
```
**拦截型（block）必须把「本欲 X → 改 Y」写进场景列**（如「本欲直接发草稿 → 改成只交付不动手」）。约束型红线命中只能是这种，不记 = 永远 0。

**引导动作**：说明格式后，问「要不要现在记一条示例（比如刚走过的某个红线）？」若用户给，则写入一条示例行；否则说明「以后每次命中随手追加即可」。

---

## 阶段 C：关云端 + hook（指引）

### 关闭云端记忆（手动，模型只给指引）
不同客户端位置不同，示例（WorkBuddy）：在客户端设置中关闭「云端记忆 / 用户画像自动生成」类开关；确认会话开始时自动注入的只剩本地 `MEMORY.md`（及 hook 注入内容），不再有远端画像。**关闭后重启客户端生效。**（这一步需你亲手在 UI 操作，模型无法代关。）

### SessionStart hook 配置（已验证 schema）
WorkBuddy 的 hook 配置位于 `~/.workbuddy/settings.json`，结构如下（直接复用本 skill 的脚本即可）：
```json
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "<按阶段 0 探测到的 OS 选下方写法>",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

**不同 OS 的 `command` 写法（按阶段 0 的 `OS` 取，路径里的 `<用户名>` 换成真实用户名）**：
- **macOS**：`"/Users/<用户名>/.workbuddy/hooks/session-start-inject.py"`
- **Windows**：`"python" "C:\\Users\\<用户名>\\.workbuddy\\hooks\\session-start-inject.py"`（Windows 上需显式用 `python` 调起 `.py`；路径反斜杠转义为 `\\`）
- **Linux**：`"/home/<用户名>/.workbuddy/hooks/session-start-inject.py"`
**机制**：宿主在会话创建、模型读第一条消息前执行该脚本；脚本读取 `FILES` 列表里的文件，向 stdout 输出 `{"continue": true, "hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": "<文件内容>"}}`，宿主把 `additionalContext` 注入上下文。**只注入、无副作用、不抛异常、exit 0。** 想强制加载哪个分叉，就把它加进脚本的 `FILES`。

**引导动作**：打印上述配置块，说明「这是手动添加步骤」。若用户明确说「帮我加进去 / 授权你改」，则先备份 `settings.json` 再做带备份的合并；否则只提示，不改文件。

---

## 文件说明

| 文件 | 作用 |
|---|---|
| `MEMORY.md`（路由层） | 只放六分叉路由指针 + 每会话硬红线 + 红线落点索引 + 写记忆规则指针。**不堆细则。** |
| `knowledge/01-dialogue.md` | 分叉①：我要「说」一句话 |
| `knowledge/02-output.md` | 分叉②：我要「造」一个东西 |
| `knowledge/03-calc.md` | 分叉③：我要「算」出一个数 |
| `knowledge/04-local.md` | 分叉④：我要「动」一下现实 |
| `knowledge/05-lookup.md` | 分叉⑤：我要「查」一个事实 |
| `knowledge/06-system.md` | 分叉⑥：我要「改我自己」 |
| `memory-usage-ledger.md` | memory 台账：记录每条红线命中/拦截，供定期人工审核 |
| `hooks/session-start-inject.py` | SessionStart hook 脚本模板：读 FILES → stdout 吐注入 JSON |

## 六分叉路由原则
- 分类依据 = **「触发后我要做的那个动作」**，不是内容主题。
- **上级节点必须完全包含下级节点**（如：改动文件 ⊇ 编辑文件 + 生成新文件）。
- 主 `MEMORY.md` 只放**路由指针**，细则全在二级文件；只有基础红线靠 hook 强制加载。
- 路由判据：先判断这一步属于哪个分叉，再去读对应二级文件。拿不准归哪类时，按索引读对应分叉，不要凭类别名猜。

## memory 台账与人工审核（核心，无法自动化）
- 每条红线命中/拦截都追加一行：`YYYY-MM-DD | R<编号> | hit/block | <场景>`。
- 拦截型场景（block）必须把「本欲 X → 改 Y」写进场景列。约束型红线命中只能是这种，不记 = 永远 0。
- 定期（建议每周）**人工**过一遍台账：哪条红线长期 0 命中？是否该裁撤；哪条频繁拦截却仍出错？说明规则有漏洞要修。
- **记忆修复必须人工审核**——这是与「关掉的云端自动生成」的根本区别，切勿试图用自动评分替代。

## 可移植性与限制
- 仅验证于 WorkBuddy 本地 memory 体系；其他客户端若没有 `SessionStart` hook 或路径不同，需自行适配。
- 本 skill 分发的是**框架与骨架 + 引导流程**，不是某人的私人记忆内容。
- 阶段 A 只铺文件；阶段 B 半自动（你确认才落盘）；阶段 C 不擅自改你的设置（除非你明确授权）。

## 发布到 GitHub（供你自行操作）
见同目录 `PUBLISH.md`——包含初始化仓库、提交、在 GitHub 建 repo 并 push，以及别人如何安装（`npx skills add <你>/memory-fork-routing --skill memory-fork-routing`）的步骤。**本 skill 不会自动上传任何内容。**
