# ③ 计算与研判（calc）

> 分叉定位：触发后我要做的那个动作是「**算**出一个数、给出量化结论」。
> 动作产出的是一个数值或量化判断。

## 本分叉承载什么
- 涉及钱、量、阈值、占比等计算时的防错协议（如多方分账、含税换算、倒推）。
- 计算前的数据口径确认、计算后的交叉验证要求。

## 填写指引
- 把这类场景下你的**计算纪律、校验规则、易错点**写在这里。
- 涉及脚本/工具报出的上限、阈值、计数、版本类数字，引用前先回源核真值（见分叉⑤）。
- 若希望强制加载，把它加入 hook 脚本的 `FILES`。

## 你的规则
（在此填写）
